# Universo Platformo Ruby

Реализация Universo Platformo / Universo MMOOMM / Universo Kiberplano на базе Ruby on Rails и связанного стека Ruby.

## Обзор

Universo Platformo Ruby — это реализация экосистемы Universo Platformo на Ruby on Rails, модульной платформы для создания распределённых приложений с кластерами, метавселенными и мультиплеерными возможностями. Эта реализация следует лучшим практикам Ruby on Rails, сохраняя концептуальное соответствие с референсной реализацией [Universo Platformo React](https://github.com/teknokomo/universo-platformo-react).

## Статус проекта

🚧 **В разработке** - Фаза начальной настройки

Проект находится на стадии начальной настройки. Структура репозитория, документация и базовая инфраструктура разрабатываются в соответствии с конституцией проекта и спецификациями.

## Архитектура

### Структура монорепозитория

Проект использует структуру монорепозитория с пакетами, организованными в директории `packages/`:

```
packages/
├── clusters-frt/     # Пакет фронтенда кластеров
│   └── base/         # Базовая реализация
├── clusters-srv/     # Пакет бэкенда кластеров
│   └── base/         # Базовая реализация
└── ...               # Дополнительные пакеты функций
```

### Соглашение об именовании пакетов

- **Пакеты фронтенда**: `<функция>-frt` (например, `clusters-frt`)
- **Пакеты бэкенда**: `<функция>-srv` (например, `clusters-srv`)
- Каждый пакет содержит директорию `base/` для базовых реализаций, что позволяет создавать альтернативные реализации в будущем

### Технологический стек

- **Язык**: Ruby 3.2+
- **Фреймворк**: Ruby on Rails 7.0+
- **База данных**: PostgreSQL через Supabase
- **Аутентификация**: Supabase Auth
- **Тестирование**: RSpec, FactoryBot, Capybara
- **Качество кода**: RuboCop, Brakeman, Bundler-audit
- **Фронтенд**: ViewComponent со стилями Material Design

## Требования

- Ruby 3.2 или выше
- PostgreSQL (через Supabase)
- Bundler
- Node.js (для компиляции ресурсов)

## Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/teknokomo/universo-platformo-ruby.git
cd universo-platformo-ruby
```

### 2. Установка зависимостей

```bash
bundle install
```

### 3. Настройка окружения

Скопируйте файл примера окружения и настройте параметры:

```bash
cp .env.example .env
```

Отредактируйте `.env` с вашими учётными данными Supabase:

```
SUPABASE_URL=ваш_url_supabase
SUPABASE_KEY=ваш_ключ_supabase
DATABASE_URL=ваш_url_базы_данных
```

### 4. Настройка базы данных

```bash
rails db:create
rails db:migrate
rails db:seed
```

### 5. Запуск приложения

```bash
rails server
```

Откройте `http://localhost:3000` в браузере.

## Разработка

### Запуск тестов

```bash
# Запуск всех тестов
bundle exec rspec

# Запуск конкретного файла теста
bundle exec rspec spec/models/cluster_spec.rb

# Запуск с покрытием
COVERAGE=true bundle exec rspec
```

### Качество кода

```bash
# Запуск линтера RuboCop
bundle exec rubocop

# Запуск проверок безопасности
bundle exec brakeman

# Проверка уязвимых зависимостей
bundle exec bundle-audit check --update
```

## Структура проекта

```
.
├── .github/              # Конфигурации и workflows GitHub
│   └── instructions/     # Рекомендации для issues, PR, меток
├── .specify/             # Инструменты Specify AI
│   ├── memory/          # Конституция проекта
│   └── templates/       # Шаблоны документов
├── packages/            # Пакеты функций (структура монорепозитория)
├── specs/               # Спецификации функций
├── config/              # Конфигурация Rails
├── app/                 # Код приложения Rails
├── db/                  # Миграции и seeds базы данных
└── spec/                # Набор тестов
```

## Основные функции

### Кластеры

Базовая функция, реализующая трёхуровневую иерархию:

- **Кластеры**: Организационные единицы верхнего уровня
- **Домены**: Единицы среднего уровня внутри кластеров
- **Ресурсы**: Отдельные ресурсы внутри доменов

Эта структура служит шаблоном для других функций, таких как Метавселенные (Метавселенные/Секции/Сущности), и обеспечивает согласованный паттерн во всей платформе.

## Документация

- **[Конституция](/.specify/memory/constitution.md)**: Основные принципы и архитектурные решения
- **[Спецификации](/specs/)**: Детальные спецификации функций
- **[Рекомендации GitHub](/.github/instructions/)**: Workflows для issues, PR и меток
- **[Руководство по разработке](DEVELOPMENT.md)**: Подробные инструкции по разработке (скоро)

## Интернационализация

Этот проект поддерживает несколько языков, при этом английский является основным стандартом:

- Все комментарии к коду и встроенная документация на английском языке
- README файлы существуют как на английском (README.md), так и на русском (README-RU.md)
- Текст UI использует фреймворк Rails I18n с локалями `en` и `ru`
- GitHub Issues включают русские переводы в разделах со спойлерами

## Участие в проекте

1. Прочитайте [Инструкции GitHub](/.github/instructions/)
2. Создайте Issue, используя предоставленные шаблоны
3. Создайте feature branch из вашего Issue
4. Отправьте Pull Request, следуя рекомендациям по PR
5. Убедитесь, что все тесты проходят и проверки качества кода успешны

## Референсная реализация

Этот проект вдохновлён и поддерживает концептуальное соответствие с [Universo Platformo React](https://github.com/teknokomo/universo-platformo-react), адаптируя его архитектуру к лучшим практикам Ruby on Rails.

## Лицензия

[Информация о лицензии будет добавлена]

## Ссылки

- **Документация**: [docs.universo.pro](https://docs.universo.pro) (скоро)
- **Реализация React**: [universo-platformo-react](https://github.com/teknokomo/universo-platformo-react)
- **Веб-сайт**: [universo.pro](https://universo.pro) (скоро)
